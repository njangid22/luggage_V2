# Terminal Nine
A small browser game about moving luggage into storage.

## Setup
```powershell
npm install
npm run dev
```

If port 3000 is busy, change the `dev` script in `package.json` or use another port.
