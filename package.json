{
    "name": "terminal-nine",
    "version": "0.0.0",
    "private": true,
    "devDependencies": {
        "typescript": "^5.0.0"
    },
    "scripts": {
        "build": "tsc -p .",
        "dev": "npm run build && npx http-server -p 3000 -c-1 ."
    }
}